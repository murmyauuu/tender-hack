# B01 — handoff

- Status: done
- Owner / task: Стас / агент B / B01 — Frontend на frozen fixtures
- BASE_SHA: `dddb724f454c986a60c6b3243db41f22bfbfabea` (`origin/main` после `git fetch --prune origin` 2026-09-12)
- Result/content SHA: `1e920cd3e8cda0225078ff68d7f6b549ed3408ba`
- Branch: `task/b01`
- Worktree: `C:\Users\Стасян\Desktop\tendhack\tender-hack-b01`
- Contracts: `2.0.0-c0`; generated input `contracts/openapi/openapi.json`

## Changed files

- `frontend/package.json`, `frontend/package-lock.json`, `frontend/tsconfig.json`, `frontend/vite.config.ts`
- `frontend/index.html`, `frontend/README.md`
- `frontend/src/App.tsx`, `frontend/src/main.tsx`, `frontend/src/styles.css`, `frontend/src/vite-env.d.ts`
- `frontend/src/mockTransport.ts`
- `frontend/src/App.test.tsx`, `frontend/src/mockTransport.test.ts`, `frontend/src/test/setup.ts`
- `frontend/src/generated/**` — generated by `@hey-api/openapi-ts`, not manually edited
- `docs/coordination/stas/CR-B01.md`
- `docs/coordination/stas/B01-handoff.md` (delivery commit after content SHA)

## Implemented UI states

- chat shell, history, input, send action and new topic action;
- queued, retrieval progress and candidate source explicitly marked as not yet an answer;
- normal RAG answer, source trigger/drawer, source → conditions → steps order;
- clarification;
- handoff offered with explicit consent action and no premature Ticket;
- Ticket with text «В очереди специалиста нашего сервиса» and unknown recipient fallback;
- operator answer in the same chat;
- policy closure;
- generic retryable error;
- stale/case-version error preserving user intent;
- feedback to a concrete message: useful is separate from solved, the first useful click immediately records canonical `FeedbackInput`, and specialist rating is rendered only for a genuine `responder_type=operator` + `answer_origin=operator` answer.

The UI does not render retrieval scores, prompts, VRAM, trace/debug/reason internals or claim Portal support integration. All assets/styles are local; there is no CDN, secret, SQLite/Ollama access, operator cabinet, dashboard or browser policy logic.

## Frozen fixtures rendered

All eight shared fixtures are imported directly from `contracts/fixtures` and covered by tests:

1. `answer.json`
2. `clarify.json`
3. `handoff_offered.json`
4. `ticket.json`
5. `operator.json`
6. `policy.json`
7. `error.json`
8. `stale.json`

Typed transitional `RequestView` states for queued/retrieving/candidate are mock-only derivatives anchored to IDs from `answer.json`; they are not claimed as additional frozen fixtures.

## Commands and actual results

- `npm install` → success; 150 packages audited after removing the unnecessary deprecated runtime client package. Full install reports four high advisories in the dev-only OpenAPI generator chain; `npm audit --omit=dev` → `found 0 vulnerabilities`.
- `npm run generate:api` → success; `@hey-api/openapi-ts v0.99.0`, generated client/types from C0 OpenAPI.
- `npm run check:generated` → success; regeneration left `frontend/src/generated` unchanged.
- `npm run typecheck` → success; `tsc --noEmit`, exit 0.
- `npm test` → success; 2 files, 8 tests passed. Tests render all eight frozen fixtures plus queued/retrieval/candidate and verify feedback/source/order/forbidden-field behavior.
- `npm run build` → success; Vite 8.3.0, 25 modules; `dist/index.html` 0.48 kB, CSS 6.52 kB, JS 238.72 kB; build time 317 ms in the final combined run.
- `git diff --cached --check` and scope check before content commit → success; only `frontend/**` and `docs/coordination/stas/**`.

## Mock / real mode

Mock only. The header permanently says «Демо на mock-данных». `MockTransport` never calls `fetch`; no real backend or E2E is connected. The generated C0 SDK exists for B02 but is not invoked by B01.

## Blockers / contract change request

No blocker for the completed honest B01 mock UI. `docs/coordination/stas/CR-B01.md` records a C0 coverage gap: no frozen `RequestView` transition fixtures, no frozen `SourceRecord`, and no public structured summary/conditions/steps payload. B01 therefore does not invent URL/PDF/date/version/conditions: the drawer and conditions section explicitly report unavailable fixture data.

## Inputs required for B02

- accepted integration SHA containing this B01 result and the backend API-ready SHA;
- A's decision on `CR-B01` or canonical real API behavior for `SourceRecord` and structured answer content;
- real API base URL/dev origin and HttpOnly-cookie/session setup;
- confirmed polling/idempotency/version-conflict behavior for the generated nine-operation C0 SDK;
- backend states for request progress, case refresh, handoff and feedback. B02 must replace the mock wiring without mixing mock and real modes.
