# Explicitly choose one transport. Any value other than "real" keeps the frozen mock UI.
VITE_API_MODE=mock

# In development leave this empty to use the same-origin Vite proxy.
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
