"""Отчёт D02: детерминированные report-функции по одному EvaluationExport JSON.

Каждая метрика пересчитывается из строк экспорта (case-granularity). Все
числа — фактическое n/N из входного JSON; пустой знаменатель → value=None;
cohort (demo/live) не смешиваются. Отчёт строится скриптом, LLM/GPU не нужны.
"""