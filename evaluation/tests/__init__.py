"""Тесты D02: runner (fixture) и детерминированные report-функции.

Запуск из корня репозитория:
    python -m pytest tests evaluation/tests
    (bare `pytest` видит только tests/ из-за testpaths=["tests"] в pyproject)
"""